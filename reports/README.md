# Dossier reports/

Placez ici le fichier `.pbix` du tableau de bord Power BI (fichier binaire, non généré par ce dépôt).

GitHub n'affiche pas le contenu d'un `.pbix` directement : pensez à exporter aussi 1-2 captures
d'écran haute résolution de chaque page du dashboard (ex. `dashboard_overview.png`,
`dashboard_filtered_category03.png`) pour que le rendu du projet soit visible sans ouvrir Power BI.
